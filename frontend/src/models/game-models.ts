export interface RenderObject {
    id: string
    created_at?: string
    name: string
    name_visible: boolean
    x: number
    y: number
    texture?: string
    width: number
    height: number
    solid: boolean
}
